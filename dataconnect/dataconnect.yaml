specVersion: "v1"
serviceId: "vibeverse"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vibeverse-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
